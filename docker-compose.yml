services:
  postgres:
    image: postgres:17-alpine
    container_name: pulse-watch-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: pulsewatch
      POSTGRES_PASSWORD: pulsewatch_dev
      POSTGRES_DB: pulsewatch
    ports:
      - "5432:5432"
    volumes:
      - pulse_watch_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pulsewatch -d pulsewatch"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  pulse_watch_postgres_data:
